name: Run Mocha Tests

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  # Enable manual trigger via workflow_dispatch
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18, 20] # Updated to Node.js 18 and 20

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      # Install pnpm
      - name: Install pnpm
        run: npm install -g pnpm

      # Install dependencies using pnpm
      - run: pnpm install

      # Run tests using pnpm
      - run: pnpm test
        env:
          CI: true
